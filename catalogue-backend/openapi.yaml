openapi: 3.1.0
info:
  title: FastAPI
  version: 0.1.0
paths:
  /organisations:
    get:
      summary: List Organisations
      operationId: list_organisations_organisations_get
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Organisation'
                type: array
                title: Response List Organisations Organisations Get
  /catalogue:
    get:
      summary: List Catalogue Entries
      operationId: list_catalogue_entries_catalogue_get
      parameters:
        - name: query
          in: query
          required: false
          schema:
            type: string
            title: Query
        - name: publisherID
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/organisationID'
            default: []
            title: Publisherid
        - name: resourceType
          in: query
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/resourceType'
            default: []
            title: Resourcetype
        - name: _limit
          in: query
          required: false
          schema:
            type: integer
            default: 100
            title: ' Limit'
        - name: _offset
          in: query
          required: false
          schema:
            type: integer
            default: 0
            title: ' Offset'
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DataResourceSummary'
                title: Response List Catalogue Entries Catalogue Get
        "422":
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /catalogue/{resource_id}:
    get:
      summary: Catalogue Entry Detail
      operationId: catalogue_entry_detail_catalogue__resource_id__get
      parameters:
        - name: resource_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
            title: Resource Id
      responses:
        "200":
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DataResource'
        "422":
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    ContactPoint:
      properties:
        contactName:
          anyOf:
            - type: string
            - type: "null"
          title: Contactname
        email:
          type: string
          pattern: ([A-Za-z0-9]+[.-_])*[A-Za-z0-9]+@[A-Za-z0-9-]+(\.[A-Z|a-z]{2,})+
          title: Email
          description: Valid email address
          examples:
            - user@domain.com
      type: object
      required:
        - email
      title: ContactPoint
    DataResource:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        publisher:
          $ref: '#/components/schemas/Organisation'
        title:
          type: string
          title: Title
        description:
          type: string
          title: Description
        created:
          type: string
          format: date-time
          title: Created
        type:
          $ref: '#/components/schemas/resourceType'
        modified:
          anyOf:
            - type: string
              format: date-time
            - type: "null"
          title: Modified
        alternativeTitle:
          anyOf:
            - items:
                type: string
              type: array
            - type: "null"
          title: Alternativetitle
          default: []
        issued:
          anyOf:
            - type: string
              format: date-time
            - type: "null"
          title: Issued
        accessRights:
          anyOf:
            - $ref: '#/components/schemas/rightsStatement'
            - type: "null"
        contactPoint:
          $ref: '#/components/schemas/ContactPoint'
        keyword:
          anyOf:
            - items:
                type: string
              type: array
            - type: "null"
          title: Keyword
          default: []
        relatedResource:
          anyOf:
            - items:
                type: string
                minLength: 1
                format: uri
              type: array
            - type: "null"
          title: Relatedresource
          default: []
        summary:
          anyOf:
            - type: string
            - type: "null"
          title: Summary
        theme:
          anyOf:
            - items:
                type: string
                minLength: 1
                format: uri
              type: array
            - type: "null"
          title: Theme
          default: []
        licence:
          anyOf:
            - type: string
              minLength: 1
              format: uri
            - type: "null"
          title: Licence
          default: https://www.nationalarchives.gov.uk/doc/open-government-licence/version/3/
        securityClassification:
          anyOf:
            - $ref: '#/components/schemas/securityClass'
            - type: "null"
          default: OFFICIAL
        version:
          anyOf:
            - type: string
            - type: "null"
          title: Version
          default: "1.0"
        creator:
          anyOf:
            - items:
                $ref: '#/components/schemas/Organisation'
              type: array
            - type: "null"
          title: Creator
          default: []
      type: object
      required:
        - id
        - publisher
        - title
        - description
        - created
        - type
        - contactPoint
      title: DataResource
    DataResourceSummary:
      properties:
        id:
          type: string
          format: uuid
          title: Id
        publisher:
          $ref: '#/components/schemas/Organisation'
        title:
          type: string
          title: Title
        description:
          type: string
          title: Description
        created:
          type: string
          format: date-time
          title: Created
        type:
          $ref: '#/components/schemas/resourceType'
        modified:
          anyOf:
            - type: string
              format: date-time
            - type: "null"
          title: Modified
      type: object
      required:
        - id
        - publisher
        - title
        - description
        - created
        - type
      title: DataResourceSummary
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    Organisation:
      properties:
        title:
          type: string
          title: Title
        id:
          type: string
          title: Id
        acronym:
          type: string
          title: Acronym
        homepage:
          type: string
          minLength: 1
          format: uri
          title: Homepage
      type: object
      required:
        - title
        - id
        - acronym
        - homepage
      title: Organisation
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
              - type: string
              - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
        - loc
        - msg
        - type
      title: ValidationError
    organisationID:
      type: string
      enum:
        - department-for-work-pensions
        - food-standards-agency
        - nhs-digital
        - ordnance-survey
      title: organisationID
    resourceType:
      type: string
      enum:
        - dataService
        - dataset
      title: resourceType
    rightsStatement:
      type: string
      enum:
        - INTERNAL
        - OPEN
        - COMMERCIAL
      title: rightsStatement
    securityClass:
      type: string
      enum:
        - OFFICIAL
        - SECRET
        - TOP_SECRET
        - NOT_APPLICABLE
      title: securityClass
